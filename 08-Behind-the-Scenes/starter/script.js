const name = "Emon";

function calcAge(birthYear) {
    const age = 2022 - birthYear;
    return age;

    function detail() {
        const output = `M`
    }
};

const result = calcAge(1998);
console.log(result);